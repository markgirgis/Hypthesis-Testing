
public class comments {

	/*double sum = 0;
	double differenceOfSquars( List<Double> list , double mean) {
		sum=0;
		list.forEach( item -> sum += (item-mean)*(item-mean) );
		return sum;

	}*/
	
	/*double sum=0;
	for(int x = 0;x<ar.size();x++) {
	sum+=ar.get(x);
	}
	double avg = sum/ar.size();
	double sd = Math.sqrt( differenceOfSquars( ar, avg) / ar.size() );
	
	Collections.sort(ar);
	double median = ar.get( ar.size() / 2);
	
	Double maxValue = ar.get(0);
	for(int i=1;i<ar.size();i++){
		if(ar.get(i) > maxValue){
			maxValue = ar.get(i);
		}
	}
	
	Double minValue = ar.get(0);
	for(int i=1;i<ar.size();i++){
		if(ar.get(i) < minValue){
			minValue = ar.get(i);
		}
	}*/
	
	/*ListView <Double> list = new ListView<>();
	list.setItems(FXCollections.observableArrayList(ar));*/
	
	//gp.setCenter(list);

}
